name: CodeQL

on:
  push:
    branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@2aa3675d385e2dbdc1ff7838f8fa22b4735b134c # main
    secrets: inherit
    with:
      use-sap: true
