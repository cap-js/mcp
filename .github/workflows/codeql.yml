name: CodeQL

on:
  push:
    branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@87c2db57786c5525a56271a7370075ff0cf20a3d # main
    secrets: inherit
    with:
      use-sap: true
