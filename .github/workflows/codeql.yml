name: CodeQL

on:
  push:
    branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@9f7b60fd8374f26f57c64546ea8a82e7e11f6adc # main
    secrets: inherit
    with:
      use-sap: true
