name: CodeQL

on:
  push:
    branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@ffc33468176cbd4763d8d1e2556a2dfa0ae55a06 # main
    secrets: inherit
    with:
      use-sap: true
